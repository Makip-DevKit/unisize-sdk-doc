# SDK リファレンス（Android）

## ドキュメントのバージョン情報

- 26/08/13 aunn コーディネートの Class（`AunnCoordinate`、`AunnCoordinateTracking`、`AunnCoordinateMeasurement`）とモデル / enum を追加。
- 24/09/20 `UnisizeBannerListener`に`didBeidChanged()`を追加。
- 24/09/06 `UnisizeBannerListener`の`didResized()`の戻り値について補足を追記
- 24/06/28 Android Java 対応に伴い、Java のサンプルコード追加
- 24/06/24 UnisizeBanner Class に CI バナー関連の情報を追加
- 24/05/27 UnisizeCVTag Class の`setupParam()`メソッドの「実装例」更新
- 24/05/20 初版

---

## UnisizeBanner Class

| Class         | 継承    |
| :------------ | :------ |
| UnisizeBanner | Webview |

unisize バナー表示、unisize アンケート画面表示用 Class。
EC アプリケーションの商品詳細画面の Activity（または、Fragment）に実装することで、unisize バナーを表示することができます。

### メソッド、プロパティ

| メソッド、プロパティ                                             | 説明                                                                                                                     | 種類       |
| :--------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------- | :--------- |
| [setupParam()](#setupparam-メソッド)                             | UnisizeBanner Class のパラメーターを設定します。                                                                         | メソッド   |
| [addInterfaceToJavaScript()](#addinterfacetojavascript-メソッド) | JavaScript とネイティブコード間のインタラクションを設定します。                                                          | メソッド   |
| [show()](#show-メソッド)                                         | バナーを表示します。                                                                                                     | メソッド   |
| [listener](#listener-プロパティunisizebannerlistener)            | UnisizeBanner class の完了時、バナーリサイズ時、エラー発生時などのイベントをハンドリングするためのリスナーを設定します。 | プロパティ |

### setupParam() メソッド

UnisizeBanner Class のパラメーターを設定します。
このタイミングでバナーの表示に必要なアイテム識別 ID（商品 ID）、クライアント ID などのパラメーターを渡します。
（itm、cuid など商品やログインの有無で可変する値以外はアクティビティのレイアウトファイル（xml ファイル）に直接設定することも可能です。）

| パラメーター     | 型                  | 初期値 | 説明                                                                                                                                                                                                                                     |
| :--------------- | :------------------ | :----- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| bannerType       | BannerType?         | -      | バナーの種類を指定します。<br>TEXT バナー：jp.co.makip.unisizesdk.UnisizeBanner.BannerType.TEXT<br>EX バナー：jp.co.makip.unisizesdk.UnisizeBanner.BannerType.EX<br>CI バナー：jp.co.makip.unisizesdk.UnisizeBanner.BannerType.CI        |
| bannerMode       | List\<BannerType\>? | -      | バナーモード（※下記サンプルコード参照）                                                                                                                                                                                                  |
| cid              | String?             | -      | クライアント ID                                                                                                                                                                                                                          |
| itm              | String?             | -      | アイテム識別 ID（商品 ID）                                                                                                                                                                                                               |
| cuid             | String?             | -      | クライアントユーザー ID（EC アプリケーション（サイト）のユーザーログイン ID） <br>※ ユーザーが EC アプリケーションにログインしている場合はこのパラメーターもセットします。                                                               |
| lang             | String?             | -      | unisize の初期表示言語<br>※ デフォルトは日本語：ja                                                                                                                                                                                       |
| enableWebViewLog | Bool?               | -      | UnisizeBanner Class で使用している Webview の console.log の内容を Android Studio の Logcat に出力します。<br>※リリース時は `false` を指定してください。                                                                                 |
| enablePrintLog   | Bool?               | -      | UnisizeBanner Class のデバッグログを Android Studio の Logcat に出力します。<br>※ リリース時は `false` を指定してください。                                                                                                              |
| sendErrorLog     | Bool?               | -      | エラー発生時にエラーログを unisize サーバーへ送信します。<br>※ エラーロギングについては付属のドキュメント「Readme_android」の「エラーロギングについて」をご覧ください。                                                                  |
| customStyle      | String?             | -      | バナーにカスタムスタイルを適用する場合に使用します。<br>css を渡すことでバナーにカスタムスタイルを適用できます。<br>詳しくは付属のドキュメント Readme_android_kotlin.pdf の「バナーのカスタムスタイル適用について」を参照ください。（※） |

- unisize では、バナーにカスタムスタイルを適用することで、unisize バナーのバージョンアップ時に表示崩れや不具合の原因となる可能性があるなどの点から、バナーのカスタマイズを推奨していません。ブランドイメージや、アプリの UI との兼ね合いで、どうしてもバナーカスタマイズが必要な場合に限定してご利用ください。

###### Kotlin

```kotlin
textWebView = findViewById<jp.co.makip.unisizesdk.UnisizeBanner>(R.id.textWebView)

private val sdk_bnrmode = listOf(
    jp.co.makip.unisizesdk.UnisizeBanner.BannerType.TEXT,
    jp.co.makip.unisizesdk.UnisizeBanner.BannerType.EX
)

textWebView.setupParam(
    bannerType = UnisizeBanner.BannerType.TEXT,
    bannerMode = sdkBnrMode,
    itm = itm,
    cid = cid,
    cuid = cuid,
    lang = lang,
    enableWebViewLog = true,
    enablePrintLog = true,
    sendErrorLog = true,
    customStyle = customStyle
)
```

###### Java

```Java
private jp.co.makip.unisizesdk.UnisizeBanner textWebView;

private final java.util.List<jp.co.makip.unisizesdk.UnisizeBanner.BannerType> sdkBnrMode =
    java.util.Arrays.asList(
        jp.co.makip.unisizesdk.UnisizeBanner.BannerType.TEXT,
        jp.co.makip.unisizesdk.UnisizeBanner.BannerType.EX
    );

textWebView = findViewById(R.id.textWebView);

textWebView.setupParam(
    jp.co.makip.unisizesdk.UnisizeBanner.BannerType.TEXT,
    sdkBnrMode,
    itm,
    cid,
    cuid,
    lang,
    true,
    true,
    true,
    customStyle
);
```

### addInterfaceToJavaScript() メソッド

WebView コンポーネントを使って JavaScript とネイティブコード間のインタラクションを設定するためのメソッド。
setupParam() メソッドを実行した後に実行します。

| パラメーター | 型      | 初期値 | 説明                                                                   |
| :----------- | :------ | :----- | :--------------------------------------------------------------------- |
| context      | Context | -      | 現在のアプリケーション、またはアクティビティのコンテキストを渡します。 |

### show() メソッド

バナーを画面上に表示します。

- パラメーターなし

### listener プロパティ（UnisizeBannerListener）

UnisizeBanner class の完了時、バナーリサイズ時、エラー発生時などに、EC アプリケーション側で追加で処理を行う場合は、listener 内に実装を行います。
バナーサイズ確定、バナー表示時に UI を調整したり、エラー発生時にユーザーに対して通知するといった場合に有用です。

###

| メソッド                                                                                                                               | 継承                                                                                           |
| :------------------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------- |
| [didFinish()](#didfinish)                                                                                                              | バナーの読み込み完了時に実行したい処理を実装します。                                           |
| [didFail(unisizeError: UnisizeError)](#didfailunisizeerror-unisizeerror)                                                               | エラーが発生した場合に実行したい処理を実装します。                                             |
| [didResized(width: Int, height: Int)](#didresizedwidth-int-height-int)                                                                 | バナーサイズが確定したタイミングで実行したい処理を実装します。                                 |
| [bannerClicked()](#bannerclicked)                                                                                                      | ユーザーがバナーをクリックしたタイミングで実行したい処理を実装します。                         |
| [didUnsupported(message: String)](#didunsupportedmessage-string)                                                                       | unisize 対象外商品の場合に実行したい処理を実装します。                                         |
| [didBeidChanged(beid: String, recommendedItems: String, type: String)](#didbeidchangedbeid-string-recommendeditems-string-type-string) | 【v1.3 追加】unisize の beid など unisize の CV タグに必要なデータの値の更新時に実行されます。 |

#### didFinish()

バナーの読み込み完了時に実行したい処理を実装します。

- パラメーターなし。

###### kotlin

```kotlin
textWebView.listener = object: jp.co.makip.unisizesdk.UnisizeBannerListener {
    override fun didFinish() {
        ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

###### Java

```Java
textWebView.setListener(new jp.co.makip.unisizesdk.UnisizeBannerListener() {
    @Override
    public void didFinish() {
        // ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

#### didFail(unisizeError: UnisizeError)

エラーが発生した場合に実行したい処理を実装します。

| パラメーター | 型           | 説明                                                                                                                                        |
| :----------- | :----------- | :------------------------------------------------------------------------------------------------------------------------------------------ |
| unisizeError | UnisizeError | UnisizeBanner クラスから返された UnisizeError class を取得できます。<br>（※ UnisizeError class の変数、関数は下記 UnisizeError class 参照） |

###### kotlin

```kotlin
textWebView.listener = object: jp.co.makip.unisizesdk.UnisizeBannerListener {
    override fun didFail(unisizeError: jp.co.makip.unisizesdk.UnisizeError) {
        ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

###### Java

```Java
textWebView.setListener(new jp.co.makip.unisizesdk.UnisizeBannerListener() {
    @Override
    public void didFail(jp.co.makip.unisizesdk.UnisizeError unisizeError) {
        ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

#### didResized(width: Int, height: Int)

バナーサイズが確定したタイミングで実行したい処理を実装します。
unisize では、アンケート前、アンケート後、比較アイテム登録後、ログイン、ログアウト後などで、バナーの表示が切り替わるため、バナーのデザインによっては、切り替わったタイミングでバナーの高さが変わる場合があります。そのため正しくバナーを表示、リサイズするために必須の処理となります。

| パラメーター | 型  | 説明                                                                                                                                                                        |
| :----------- | :-- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| width        | Int | バナーレンダリング完了時に確定した width を取得できます。<br>（幅はバナータイプによって変動することはありません。そのためこのパラメーターは通常使用することはありません。） |
| height       | Int | バナーレンダリング完了時に確定した height を取得できます。<br>（高さはバナータイプによって変動します。）                                                                    |

**補足：**
Android 版の場合、画面の密度（DPI のスケールファクター）を元に算出した height を返しているため、端末（モデル）ごとに異なるサイズになります。
これはバナーサイズの変更を行う際に、画面の密度も加味した height を指定しないと、端末ごとに正しいバナーサイズが適用されないためです。
そのため高さ × 画面の密度の値が、didResized の返り値で取得できるかと思います。
（iOS 版と比べ値が大きくなるのは上記の理由によります。）

取得した height はそのまま適用していただく形で問題ありません。正しいバナーの高さにリサイズされます。

###### kotlin

```kotlin
textWebView.listener = object: jp.co.makip.unisizesdk.UnisizeBannerListener {
    override fun didResized(width: Int, height: Int) {
        ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

###### Java

```Java
textWebView.setListener(new jp.co.makip.unisizesdk.UnisizeBannerListener() {
    @Override
    public void didResized(int width, int height) {
        ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

#### bannerClicked()

ユーザーがバナーをクリックしたタイミングで実行したい処理を実装します。

###### kotlin

```kotlin
textWebView.listener = object: jp.co.makip.unisizesdk.UnisizeBannerListener {
    override fun bannerClicked() {
        ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

###### Java

```Java
textWebView.setListener(new jp.co.makip.unisizesdk.UnisizeBannerListener() {
    @Override
    public void bannerClicked() {
        ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

#### didUnsupported(message: String)

unisize 対象外商品の場合に実行したい処理を実装します。

| パラメーター | 型     | 説明                                 |
| :----------- | :----- | :----------------------------------- |
| message      | String | unisize 対象外商品の情報を返します。 |

###### kotlin

```kotlin
textWebView.listener = object: jp.co.makip.unisizesdk.UnisizeBannerListener {
    override fun didUnsupported(message: String)  {
        ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

###### Java

```Java
textWebView.setListener(new jp.co.makip.unisizesdk.UnisizeBannerListener() {
    @Override
    public void didUnsupported(String message) {
        ・・・・ ここに記述
    }
    ・・・・ その他のlistener
}
```

#### didBeidChanged(beid: String, recommendedItems: String, type: String)

【v1.3 追加】unisize の beid など unisize の CV タグに必要なデータの値の更新時に実行されます。<br><br>
購入画面 → 購入完了画面が WebView で構成されているアプリの場合で、UnisizeCvTag Class を使用せずに、アプリの WebView の購入完了画面の HTML に埋め込まれている Web 版 unisize の CV タグを、アプリでも利用したい場合に使用します。<br><br>
実装方法など、詳しくは付属のドキュメント「unisize のコンバージョンの実装について」をご覧ください。

| パラメーター     | 型     | 説明                                                            |
| :--------------- | :----- | :-------------------------------------------------------------- |
| beid             | String | unisize の CV タグに必要なユーザー識別 ID を返します。          |
| recommendedItems | String | unisize の CV タグに必要なデータを情報を返します。（JSON 形式） |
| type             | String | unisize のバナータイプを返します。                              |

###### kotlin

```kotlin
textWebView.listener = object: jp.co.makip.unisizesdk.UnisizeBannerListener {
    override fun didBeidChanged(beid: String, recommendedItems: String, type: String) {
        // ログに出力
        Log.d("UnisizeBannerListener", "didBeidChanged：beid：$beid、recommendedItems：$recommendedItems")

        /*
           ここで取得した beid、recommendedItems をネイティブ側で保持して、
           アプリの購入画面の WebView に Cookie、localStorage として書き込むことで、
           WebView の購入完了画面の HTML に埋め込まれている unisize の CV タグを、
           アプリでも利用したい場合に対応できるようになります。
        */

        ・・・・

    }
    ・・・・ その他のlistener
}
```

###### Java

```Java
textWebView.setListener(new jp.co.makip.unisizesdk.UnisizeBannerListener() {
    @Override
    public void didBeidChanged(String beid, String recommendedItems, String type) {

        // ログに出力
        Log.d("UnisizeBannerListener", "didBeidChanged: beid: " + beid + ", recommendedItems: " + recommendedItems);

        /*
           ここで取得した beid、recommendedItems をネイティブ側で保持して、
           アプリの購入画面の WebView に Cookie、localStorage として書き込むことで、
           WebView の購入完了画面の HTML に埋め込まれている unisize の CV タグを、
           アプリでも利用したい場合に対応できるようになります。
        */

        ・・・・
    }
    ・・・・ その他のlistener
}
```

##### アプリの購入画面の WebView で 取得した値を localStorage、Cookie としてセットする

上記 Listener で取得した beid、recommendedItems を、ネイティブ側で変数として持ち回り、アプリの購入画面の UI 表示のタイミングに、アプリの購入画面の WebView に localStorage、Cookie として WebView にセットしてください。localStorage、Cookie の key は下記表をご覧ください。<br>

- アプリの購入完了画面の WebView の HTML に埋め込まれている Web 版 unisize の CV タグの読み込み前までにセットする必要があります。

| 種類         | 変数（値）       | Key 名                      | 用途                                                |
| :----------- | :--------------- | :-------------------------- | :-------------------------------------------------- |
| Cookie       | beid             | \_unisize_beid              | ユーザー固有のユニーク ID                           |
| localStorage | beid             | \_unisize_beid              | ユーザー固有のユニーク ID                           |
| localStorage | recommendedItems | \_unisize_recommended_items | 推奨アイテムの情報を保存するために使用（JSON 形式） |

**WebView に localStorage を書き込むためのサンプルコード**

```kotlin
var fixedBeid: String = ""
if (beid == null) {
	fixedBeid = ""
} else {
	fixedBeid = beid
}

var fixedRecommendedItems: String = ""
if (recommendedItems == null) {
	fixedRecommendedItems = ""
} else {
	fixedRecommendedItems = recommendedItems
}

if (purchaseWebView != null) {
	val jsCode = """
		var date = new Date();
		date.setTime(date.getTime() + (365 * 24 * 60 * 60 * 1000));
		var expires = "; expires=" + date.toUTCString();

		document.cookie = "_unisize_beid=$fixedBeid; path=/; expires=" + expires;

		if (typeof(Storage) !== "undefined") {
			localStorage.setItem('_unisize_beid', '$fixedBeid');
			localStorage.setItem('_unisize_recommended_items', '$fixedRecommendedItems');
		}
	""".trimIndent()

	activity?.runOnUiThread {
		purchaseWebView?.evaluateJavascript(jsCode, null)
	}
}

```

## UnisizeBanner Class の設置について

EC アプリケーションの商品詳細画面の Activity（または、Fragment）の xml ファイルに、unisize バナーを表示したい箇所に下記を追加します。

### TEXT バナー：

```xml
<jp.co.makip.unisizesdk.UnisizeBanner
    android:id="@+id/textWebView"
    android:layout_width="match_parent"
    android:layout_height="60dp" />
```

### EX バナー：

```xml
<jp.co.makip.unisizesdk.UnisizeBanner
    android:id="@+id/exWebView"
    android:layout_width="match_parent"
    android:layout_height="138dp" />
```

### CI バナー：

```xml
<jp.co.makip.unisizesdk.UnisizeBanner
    android:id="@+id/ciWebView"
    android:layout_width="match_parent"
    android:layout_height="400dp" />
```

- layout_height は初期値として指定してください。バナーのサイズ確定時に適切なサイズに調整されます。
- layout_marginLeft、layout_marginTop、layout_marginRight、layout_marginBottom は適切なサイズを指定してください。

### レイアウトから UnisizeBanner ビューを取得

###### Kotlin

```kotlin
val textWebView = findViewById<jp.co.makip.unisizesdk.UnisizeBanner>(R.id.textWebView)

// UnisizeBanner からのさまざまなイベントを処理するためのリスナーを設定
textWebView.listener = object : jp.co.makip.unisizesdk.UnisizeBannerListener {
　・・・リスナーを設定
}
```

###### Java

```java

private jp.co.makip.unisizesdk.UnisizeBanner textWebView;

・・・

textWebView = findViewById(R.id.textWebView);

・・・

textWebView.setListener(new jp.co.makip.unisizesdk.UnisizeBannerListener() {
    　・・・リスナーを設定
}
```

### インスタンス化してレイアウトに追加する場合

レイアウトに xml を使用しないで、インスタンス化してレイアウトに追加する場合、以下の手順に従って Kotlin で実装することができます。

下記は例として MainActivity に実装した場合の簡単な参考例です。

###### Kotlin

```kotlin
import android.app.Activity
import android.os.Bundle
import android.widget.LinearLayout
import jp.co.makip.unisizesdk.UnisizeBanner
import android.view.ViewGroup.LayoutParams

class MainActivity : Activity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        // LinearLayout を作成
        val layout = LinearLayout(this).apply {
            orientation = LinearLayout.VERTICAL
            layoutParams = LayoutParams(LayoutParams.MATCH_PARENT, LayoutParams.MATCH_PARENT)
        }

        // UnisizeBanner のインスタンスを作成
        val textWebView = UnisizeBanner(this).apply {
            layoutParams = LayoutParams(LayoutParams.MATCH_PARENT, 60.dpToPx())
        }

        // レイアウトにビューを追加
        layout.addView(textWebView)

        // アクティビティのコンテントビューを設定
        setContentView(layout)

        // UnisizeBanner 表示に必要な処理
        ・・・・
    }

    // ピクセル単位でdpを変換する拡張関数
    private fun Int.dpToPx(): Int = (this * resources.displayMetrics.density).toInt()
}
```

###### Java

```Java
import android.app.Activity;
import android.os.Bundle;
import android.widget.LinearLayout;
import jp.co.makip.unisizesdk.UnisizeBanner;
import android.view.ViewGroup.LayoutParams;

public class MainActivity extends Activity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        // LinearLayout を作成
        LinearLayout layout = new LinearLayout(this);
        layout.setOrientation(LinearLayout.VERTICAL);
        layout.setLayoutParams(new LayoutParams(LayoutParams.MATCH_PARENT, LayoutParams.MATCH_PARENT));

        // UnisizeBanner のインスタンスを作成
        UnisizeBanner textWebView = new UnisizeBanner(this);
        LinearLayout.LayoutParams textWebViewParams = new LinearLayout.LayoutParams(LayoutParams.MATCH_PARENT, dpToPx(60));
        textWebView.setLayoutParams(textWebViewParams);

        // レイアウトにビューを追加
        layout.addView(textWebView);

        // アクティビティのコンテントビューを設定
        setContentView(layout);

        // UnisizeBanner 表示に必要な処理
        // ・・・・
    }

    // ピクセル単位でdpを変換するメソッド
    private int dpToPx(int dp) {
        return (int) (dp * getResources().getDisplayMetrics().density);
    }
}

```

## UnisizeCVTag Class

| Class        | 継承 | タイプ |
| :----------- | :--- | :----- |
| UnisizeCVTag | なし | class  |

unisize のコンバージョン集計に必要な class。EC アプリケーションの購入完了画面の Activity（または、Fragment）に実装します。

### メソッド、プロパティ

| メソッド、プロパティ                                               | 説明                                                                                                                     | 種類       |
| :----------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------- | :--------- |
| [setupParam()](#setupparam-%E3%83%A1%E3%82%BD%E3%83%83%E3%83%89-1) | UnisizeBanner Class のパラメーターを設定します。                                                                         | メソッド   |
| [show()](#show-メソッド-1)                                         | バナーを表示します。                                                                                                     | メソッド   |
| [listener](#listener-プロパティunisizecvtaglistener)               | UnisizeBanner class の完了時、バナーリサイズ時、エラー発生時などのイベントをハンドリングするためのリスナーを設定します。 | プロパティ |

### setupParam() メソッド

UnisizeCVTag Class のパラメーターを設定します。
このタイミングでバナーの表示に必要なアイテム識別 ID（商品 ID）、クライアント ID などのパラメーターを渡します。

| パラメーター     | 型              | 初期値      | 説明                                                                                                                                                                                                                                  |
| :--------------- | :-------------- | :---------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| cid              | String          | -           | クライアント ID                                                                                                                                                                                                                       |
| cuid             | String?         | ""          | クライアント会員 ID（EC アプリケーション（サイト）のユーザーログイン ID） <br>※ ユーザーが EC アプリケーションにログインしている場合はこのパラメーターもセットします。                                                                |
| purchaseid       | String          | -           | 購入 ID。注文時に発行された固有の ID                                                                                                                                                                                                  |
| itemnum          | List\<String\>? | emptyList() | 購入数（アイテム毎）（※下記参照）                                                                                                                                                                                                     |
| itemid           | List\<String\>? | emptyList() | アイテム識別 ID（商品 ID）（アイテム毎）（※下記参照）                                                                                                                                                                                 |
| price            | List\<String\>? | emptyList() | 購入したアイテムの金額（アイテム毎）（※下記参照）<br>※ 値に小数点は含めないでください。（※下記参照）                                                                                                                                  |
| size             | List\<String\>? | emptyList() | 購入したアイテムのサイズ（アイテム毎）（※下記参照）                                                                                                                                                                                   |
| iteminfo         | String?         | ""          | itemnum、itemid、price、size を一つのデータ形式にまとめたデータを渡します。<br>Web 版 unisize の CV タグで data-iteminfo を使用している場合は、こちらをご利用ください。<br>使用しない場合は空欄（""）を渡してください。 （※下記参照） |
| iteminfojson     | String?         | ""          | itemnum、itemid、price、size を json 形式にまとめたデータを渡します。<br>Web 版 unisize の CV タグで data-iteminfojson を使用している場合は、こちらをご利用ください。<br>使用しない場合は空欄（""）を渡してください。 （※下記参照）   |
| regType          | String?         | ""          | ※ 未使用。空欄（""）を渡してください。                                                                                                                                                                                                |
| enableWebViewLog | Bool            | false       | UnisizeBanner Class で使用している Webview の console.log の内容を Android Studio の Logcat に出力します。<br>※リリース時は `false` を指定してください。                                                                              |
| enablePrintLog   | Bool            | false       | UnisizeBanner Class のデバッグログを Android Studio の Logcat に出力します。<br>※ リリース時は `false` を指定してください。                                                                                                           |

- CV タグのパラメーターの渡し方は、各クライアント様ごとに異なります。<br>itemnum、itemid、price、size を使用する方法、iteminfo、iteminfojson で渡す方法があります。<br>基本的に Web 版と同様のパラメーターにデータをセットして受け渡す形で問題ありませんが、itemnum、itemid、itemid、price は、商品ごとに List\<String\> 形式に入れて渡して下さい。unisizeSDK 内部で自動的に「#」（%23）区切りに変換して送信します。
- Shopfy など一部の EC サービスの API では、価格が小数点で送られてくる場合がございます。unisize のコンバージョン処理では小数点はサポートしていないため、setupParam() 実行前に、各商品の価格を四捨五入してから追加してください。
- コンバージョンが正しく送信されているかは、パソコンと Android 端末を USB で接続して、Chrome を使った WebView デバッグを行い、対象の WebView の「ネットワーク」タブで「tracking」という項目を確認することで、コンバージョンが送信されているかの確認や、送信される値の確認を確認できます。

#### 実装例

###### Kotlin

```kotlin
// UnisizeCVTag（WebView）の初期化と設定
// Javascriptに渡す値（※検証用のサンプルデータ）
val cid: String = "＜クライアントID＞"  // makip発行のクライアント個別ID
val cuid: String = "＜クライアント会員ID＞" // ECサイトの会員ID（注文時に発行される固有ID）
val purchaseid: String = "＜注文ID＞" // 注文時に発行される固有ID
val itemnum: List<String> = listOf("＜商品1の購入数＞", "＜商品2の購入数＞") // 購入数（購入された商品別個数）
val itemid: List<String> = listOf("＜商品1の商品識別ID＞", "＜商品2の商品識別ID＞") // 商品識別ID（購入された商品を識別するID）
val price: List<String> = listOf("＜商品1の価格＞", "＜商品2の価格＞") // 価格（購入されたアイテムの売価）
val size: List<String> = listOf("＜商品1のサイズ＞", "＜商品2のサイズ＞") // 購入されたアイテムのサイズ名称

var cvTagWebView: jp.co.makip.unisizesdk.UnisizeCVTag = findViewById(R.id.cvTagWebView)
cvTagWebView.listener = object : jp.co.makip.unisizesdk.UnisizeCVTagListener {

	・・・省略・・・
}

cvTagWebView.setupParam(
    cid,
    cuid,
    purchaseid,
    itemnum,
    itemid,
    price,
    size,
    "",
    "",
    "",
    true,
    true
)

cvTagWebView.show()
```

###### Java

```Java
// UnisizeCVTag（WebView）の初期化と設定
// Javascriptに渡す値（※検証用のサンプルデータ）
String cid = "＜クライアントID＞";  // makip発行のクライアント個別ID
String cuid = "＜クライアント会員ID＞"; // ECサイトの会員ID（注文時に発行される固有ID）
String purchaseid = "＜注文ID＞"; // 注文時に発行される固有ID
List<String> itemnum = Arrays.asList("＜商品1の購入数＞", "＜商品2の購入数＞"); // 購入数（購入された商品別個数）
List<String> itemid = Arrays.asList("＜商品1の商品識別ID＞", "＜商品2の商品識別ID＞"); // 商品識別ID（購入された商品を識別するID）
List<String> price = Arrays.asList("＜商品1の価格＞", "＜商品2の価格＞"); // 価格（購入されたアイテムの売価）
List<String> size = Arrays.asList("＜商品1のサイズ＞", "＜商品2のサイズ＞"); // 購入されたアイテムのサイズ名称

jp.co.makip.unisizesdk.UnisizeCVTag cvTagWebView = findViewById(R.id.cvTagWebView);
cvTagWebView.setListener(new jp.co.makip.unisizesdk.UnisizeCVTagListener() {
    // ・・・省略・・・
});

cvTagWebView.setupParam(
    cid,
    cuid,
    purchaseid,
    itemnum,
    itemid,
    price,
    size,
    "",
    "",
    "",
    true,
    true
);

cvTagWebView.show();
```

### show() メソッド

CV タグを画面上に表示します。
コンバージョン集計用のため、画面上には何も表示されませんが、このタイミングで集計処理が実行されます。

- パラメーターなし

### listener プロパティ（UnisizeCVTagListener）

UnisizeCVTag class の完了時、エラー発生時に、EC アプリケーション側で追加で処理を行う場合は、listener 内に実装を行います。
エラー発生時にユーザーに対して通知するといった場合に有用です。

| メソッド                                | 継承                                               |
| :-------------------------------------- | :------------------------------------------------- |
| [didFinish()]()                         | 読み込み完了時に実行したい処理を実装します。       |
| [didFail(unisizeError: UnisizeError)]() | エラーが発生した場合に実行したい処理を実装します。 |

#### didFinish()

バナーの読み込み完了時に実行したい処理を実装します。

- パラメーターなし。

###### Kotlin

```kotlin
cvTagWebView.listener = object : jp.co.makip.unisizesdk.UnisizeCVTagListener {
    override fun didFinish() {
        ・・・・ ここに記述
    }
    ・・・・ その他コード
}
```

###### Java

```Java
cvTagWebView.setListener(new jp.co.makip.unisizesdk.UnisizeCVTagListener() {
    @Override
    public void didFinish() {
        ・・・・ ここに記述
    }
    ・・・・ その他コード
});
```

#### didFail(unisizeError: UnisizeError)

エラーが発生した場合に実行したい処理を実装します。

| パラメーター | 型           | 説明                                                                                                                                        |
| :----------- | :----------- | :------------------------------------------------------------------------------------------------------------------------------------------ |
| unisizeError | UnisizeError | UnisizeBanner クラスから返された UnisizeError class を取得できます。<br>（※ UnisizeError class の変数、関数は下記 UnisizeError class 参照） |

###### Kotlin

```kotlin
cvTagWebView.listener = object: jp.co.makip.unisizesdk.UnisizeCVTagListener {
    override fun didFail(unisizeError: jp.co.makip.unisizesdk.UnisizeError) {
        ・・・・ ここに記述
    }
    ・・・・ その他コード
}
```

###### Java

```Java
cvTagWebView.setListener(new jp.co.makip.unisizesdk.UnisizeCVTagListener() {
    @Override
    public void didFail(jp.co.makip.unisizesdk.UnisizeError unisizeError) {
        ・・・・ ここに記述
    }
    ・・・・ その他コード
}
```

## AunnCoordinate Class

| Class          | 継承 | タイプ |
| :------------- | :--- | :----- |
| AunnCoordinate | なし | class  |

aunn コーディネートのコーディネート一覧を取得し、コーディネート一覧画面での行動計測を行う Class。
コーディネート一覧を表示する画面（商品詳細画面など）の Activity（または、Fragment）に実装します。コーディネート一覧の UI はホストアプリがネイティブ描画するため、この Class は画面上には何も表示しません。

- インスタンス生成時に `Context` を渡します。（例：`val coordinate = AunnCoordinate(context)`）
- 実装手順は付属のドキュメント「[11.aunn コーディネート機能の実装について（Android）](./11.aunn%20コーディネート機能の実装について（Android）.md)」をご覧ください。

### メソッド、プロパティ

| メソッド、プロパティ                                                            | 説明                                                                                     | 種類       |
| :------------------------------------------------------------------------------ | :---------------------------------------------------------------------------------------- | :--------- |
| [setupParam()](#setupparam-メソッドaunncoordinateparam)                                   | AunnCoordinate Class のパラメーターを設定します。                                        | メソッド   |
| [load()](#load-メソッド)                                                        | コーディネート一覧を取得します。                                                                 | メソッド   |
| [reload()](#reload-メソッド)                                                    | コーディネート一覧を再取得します。                                                               | メソッド   |
| [sendCoordinateViewEvent()](#sendcoordinateviewevent-メソッド)                  | コーディネート一覧を描画した時点の表示計測を送信します。                                         | メソッド   |
| [sendCoordinateViewTimeEvent()](#sendcoordinateviewtimeevent-メソッド)          | コーディネート一覧が一定時間表示された時点の表示計測を送信します。                               | メソッド   |
| [startCoordinateViewTimeTracking()](#startcoordinateviewtimetracking-メソッド)  | コーディネート一覧の表示時間計測を開始します。                                                   | メソッド   |
| [stopCoordinateViewTimeTracking()](#stopcoordinateviewtimetracking-メソッド)    | コーディネート一覧の表示時間計測を停止します。                                                   | メソッド   |
| [pauseCoordinateViewTimeTracking()](#pausecoordinateviewtimetracking-メソッド)  | コーディネート一覧の表示時間計測を一時停止します。                                               | メソッド   |
| [resumeCoordinateViewTimeTracking()](#resumecoordinateviewtimetracking-メソッド) | コーディネート一覧の表示時間計測を再開します。                                                   | メソッド   |
| [sendCoordinateLinkClickEvent()](#sendcoordinatelinkclickevent-メソッド)        | コーディネートのクリック計測を送信します。                                                       | メソッド   |
| [sendMoreLinkClickEvent()](#sendmorelinkclickevent-メソッド)                    | 「すべて見る」のクリック計測を送信します。                                               | メソッド   |
| [toggleBodyMatching()](#togglebodymatching-メソッド)                            | 体型マッチの ON / OFF を切り替えます。                                                   | メソッド   |
| [useBodyMatching](#usebodymatching-プロパティ)                                  | 体型マッチが ON かどうかを取得します。                                                   | プロパティ |
| [listener](#listener-プロパティaunncoordinatelistener)                          | コーディネート取得完了時、エラー発生時などのイベントをハンドリングするためのリスナーを設定します。 | プロパティ |

### setupParam() メソッド（AunnCoordinateParam）

AunnCoordinate Class のパラメーターを `AunnCoordinateParam` で設定します。
`load()` を呼び出す前に必ず実行してください。

| パラメーター       | 型                 | 初期値 | 説明                                                                                                                                                                                                                                                                                             |
| :----------------- | :----------------- | :----- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| cid                | String             | -      | クライアント ID（必須）                                                                                                                                                                                                                                                                          |
| pageType           | AunnCoordinatePageType       | -      | コーディネート一覧を表示する画面の種別（必須）<br>（※ 下記 AunnCoordinatePageType 参照）                                                                                                                                                                                                                           |
| coordinateCount    | Int                | -      | 画面に表示するコーディネートの件数（必須）|
| itemId             | String?            | null   | アイテム識別 ID（商品 ID）<br>※ pageType が `ITEM_DETAIL` / `COORDINATE_ITEM` の場合は必須です。                                                                                                                                                                                                |
| cuid               | String?            | null   | クライアント会員 ID（EC アプリケーション（サイト）のユーザーログイン ID）<br>※ ユーザーが EC アプリケーションにログインしている場合はこのパラメーターもセットします。                                                                                                                             |
| hideMeasurementTag | Boolean            | false  | 体型登録バナーを表示しない構成の場合に `true` を指定します。<br>※ 計測データの区分を切り替えるためのフラグです。UI の表示制御はホストアプリ側で行ってください（SDK は UI を表示しません）。                                                                                                       |
| enablePrintLog     | Boolean            | false  | AunnCoordinate Class のデバッグログを Android Studio の Logcat に出力します。<br>※ リリース時は `false` を指定してください。                                                                                                                                                                      |
| sendErrorLog       | Boolean            | true   | エラー発生時にエラーログを unisize サーバーへ送信します。                                                                                                                                                                                                                                        |
| unisizeBeidWaitMs  | Long               | 3000   | 商品詳細画面（`ITEM_DETAIL`）で、unisize バナー側の beid 発行を待つ上限（ミリ秒）。beid が発行された時点で待機は即座に終了します。<br>※ **unisize バナーを同居させない画面では `0` を指定して待機を無効化してください。**<br>※ pageType が `ITEM_DETAIL` 以外の場合、この値は使用されません。 |

###### Kotlin

```kotlin
coordinate.setupParam(
    jp.co.makip.unisizesdk.aunn.coordinate.AunnCoordinateParam(
        cid = "＜クライアントID＞",
        pageType = jp.co.makip.unisizesdk.aunn.coordinate.AunnCoordinatePageType.ITEM_DETAIL,
        coordinateCount = 6,
        itemId = "＜商品ID＞",
        cuid = "＜クライアント会員ID＞",
        enablePrintLog = true,
    ),
)
```

### load() メソッド

コーディネート一覧を取得します。取得結果は `listener` の `onCoordinatesLoaded()` へ返されます。

- パラメーターなし

### reload() メソッド

コーディネート一覧を再取得します。取得結果は `listener` の `onCoordinatesLoaded()` へ返されます。
体型登録の完了後や体型マッチの切り替え後など、コーディネートの並び順が変わるタイミングで呼び出します。

- パラメーターなし

### sendCoordinateViewEvent() メソッド

コーディネート一覧を**描画した時点**の表示計測を送信します。
コーディネートの取得に成功し、コーディネートが 1 件以上ある状態で描画できたタイミングで呼び出してください。

| パラメーター | 型                     | 説明                                                                                                            |
| :----------- | :--------------------- | :-------------------------------------------------------------------------------------------------------------- |
| recType      | AunnCoordinateRecommendAlgorithm | `onCoordinatesLoaded()` で受け取った `recType` をそのまま渡します。<br>（※ 下記 AunnCoordinateRecommendAlgorithm 参照） |

**`sendCoordinateViewTimeEvent()` との違いについて**

コーディネート一覧の表示計測には、送信条件の異なる次の 2 種類があります。送信されるデータは同じで、「どれだけの時間表示され続けたか」の値だけが異なります。**どちらか一方ではなく、両方を送信してください。**

| メソッド | 送信されるタイミング | 意味 |
| :--- | :--- | :--- |
| `sendCoordinateViewEvent()` | コーディネート一覧を描画した直後 | コーディネート一覧が**表示された** |
| `sendCoordinateViewTimeEvent()` | コーディネート一覧が 50% 以上・3 秒間表示され続けたとき | コーディネート一覧が**実際に閲覧された** |

前者は画面外（スクロールしないと見えない位置）にコーディネート一覧がある場合でも描画時点で送信しますが、後者はユーザーが実際にコーディネート一覧を目にするまで送信しません。
後者は `startCoordinateViewTimeTracking()` が可視状態を判定して自動送信するため、通常ホストアプリから直接呼び出す必要はありません。

### sendCoordinateViewTimeEvent() メソッド

コーディネート一覧が**一定時間表示された時点**の表示計測を送信します（前述の「`sendCoordinateViewTimeEvent()` との違いについて」を参照）。
通常は `startCoordinateViewTimeTracking()` が可視状態を判定して自動送信するため、**本メソッドを直接呼び出す必要はありません。** `RecyclerView` などで可視判定をホストアプリ側で行い、SDK の監視を使用しない場合にのみ呼び出してください。

| パラメーター | 型                     | 説明                                                               |
| :----------- | :--------------------- | :----------------------------------------------------------------- |
| recType      | AunnCoordinateRecommendAlgorithm | `onCoordinatesLoaded()` で受け取った `recType` をそのまま渡します。 |

### startCoordinateViewTimeTracking() メソッド

コーディネート一覧の表示時間計測を開始します。
指定した View が **50% 以上**見えている状態が **3 秒**続いたときに、表示時間計測を **1 回だけ**送信して監視を終了します。途中で 50% を下回った場合、3 秒のタイマーはリセットされます。

判定の基準となる面積は、**View 全体の面積と画面の面積のうち小さい方**です。画面より大きいコーディネート一覧を渡した場合は、画面の 50% を覆った時点で条件を満たします。

| パラメーター | 型                     | 説明                                                               |
| :----------- | :--------------------- | :----------------------------------------------------------------- |
| view         | View                   | 監視対象の View（コーディネート一覧全体のコンテナ View）を渡します。       |
| recType      | AunnCoordinateRecommendAlgorithm | `onCoordinatesLoaded()` で受け取った `recType` をそのまま渡します。 |

- `onCoordinatesLoaded()` でコーディネート一覧を描画したあとに呼び出してください。
- 監視対象の View が画面から取り外された（detach された）時点で、監視は自動的に終了します。ViewPager やフラグメントの入れ替えなどで View が再表示される場合は、あらためて本メソッドを呼び出してください。
- 可視判定は監視対象の View が画面内にあるかで行います。**上に別の UI を重ねて隠した場合は検知できない**ため、`pauseCoordinateViewTimeTracking()` / `resumeCoordinateViewTimeTracking()` を明示的に呼び出してください。
- **メインスレッドから呼び出してください。**

### stopCoordinateViewTimeTracking() メソッド

コーディネート一覧の表示時間計測を停止します。
画面の破棄時など、監視を明示的に打ち切りたい場合に呼び出します。

- パラメーターなし

### pauseCoordinateViewTimeTracking() メソッド

コーディネート一覧の表示時間計測を一時停止します。監視は維持したまま、3 秒のタイマーだけを止めます。
**コーディネート一覧の上に別の UI（体型登録アプリ・Dialog・ボトムシート・全画面オーバーレイなど）を重ねて表示している間**や、他の画面へ遷移している間に呼び出してください。

- パラメーターなし
- 可視判定はコーディネート一覧の View 自体が画面内にあるかで行うため、**上に重なったものによって隠れていることは検知できません。** 一時停止しない場合、ユーザーがコーディネート一覧を見ていない間も表示時間が計測され続けます。
- Dialog を表示しても Activity の `onPause()` は呼ばれないため、ライフサイクルのコールバックだけでは一時停止できません。コーディネート一覧を覆う UI を表示・非表示するタイミングで明示的に呼び出してください。

### resumeCoordinateViewTimeTracking() メソッド

`pauseCoordinateViewTimeTracking()` で止めた表示時間計測を再開します。
コーディネート一覧を覆っていた UI を閉じたときや、他の画面から戻ってきたときに呼び出してください。

- パラメーターなし
- 可視条件を満たしていれば、3 秒のタイマーを最初から数え直します。送信済み・未開始の場合は何も行いません。

### sendCoordinateLinkClickEvent() メソッド

コーディネートのクリック計測を送信します。
コーディネートカードがタップされたタイミングで呼び出してください。

| パラメーター  | 型  | 説明                                                          |
| :------------ | :-- | :------------------------------------------------------------ |
| coordinateId  | Int | タップされたコーディネートの ID（`AunnCoordinateItem.id`）            |
| staffId       | Int | タップされたコーディネートのスタッフ ID（`AunnCoordinateItem.staff.id`） |

### sendMoreLinkClickEvent() メソッド

「すべて見る」のクリック計測を送信します。

- パラメーターなし

### toggleBodyMatching() メソッド

体型マッチ（体型での並べ替え）の ON / OFF を切り替え、あわせて切り替えの計測を送信します。

| パラメーター | 型      | 説明                                            |
| :----------- | :------ | :---------------------------------------------- |
| enabled      | Boolean | 体型マッチを有効にする場合は `true` を渡します。 |

- **このメソッドはコーディネートの再取得を行いません。** 切り替えるとコーディネートの並び順が変わるため、呼び出し後に `reload()` を呼んでコーディネートを取り直してください。

### useBodyMatching プロパティ

体型マッチが ON かどうか（Boolean）を取得します。体型登録済みバナーのチェックボックスの状態などに使用します。

- 読み取り専用のプロパティです。値の変更は `toggleBodyMatching()` で行います。
- 初期値は `true`（ON）です。ユーザーが一度も切り替えていない状態では `true` が返ります。

### listener プロパティ（AunnCoordinateListener）

コーディネート取得の完了時、エラー発生時に、EC アプリケーション側で処理を行うため、listener 内に実装を行います。

| メソッド                                                                                     | 説明                                                       |
| :------------------------------------------------------------------------------------------- | :--------------------------------------------------------- |
| onCoordinatesLoaded(coordinates, recType, hasMore, isBodyRegistered, isBodyAndPreferenceSort) | コーディネートの取得完了時に実行したい処理を実装します。           |
| onBeidChanged(beid: String)                                                                  | beid が確定・変更されたときに実行したい処理を実装します。  |
| onFail(error: UnisizeError)                                                                  | エラーが発生した場合に実行したい処理を実装します。         |

#### onCoordinatesLoaded()

コーディネートの取得完了時に実行したい処理を実装します。取得したコーディネート配列をもとに、ホストアプリで一覧をネイティブ描画してください。

| パラメーター            | 型                        | 説明                                                                                                                                                                 |
| :---------------------- | :------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| coordinates             | List\<AunnCoordinateItem\> | 取得したコーディネートの配列（※ 下記 AunnCoordinateItem 参照）                                                                                                              |
| recType                 | AunnCoordinateRecommendAlgorithm    | 適用されたレコメンドアルゴリズム。計測メソッドへそのまま渡します。                                                                                                   |
| hasMore                 | Boolean                   | 表示件数より多くのコーディネートが存在するか。`true` の場合「すべて見る」導線を表示できます。                                                                                |
| isBodyRegistered        | Boolean                   | ユーザーが体型登録済みか。体型登録バナーの出し分け（未登録＝登録導線 / 登録済み＝変更導線）に使用します。                                                             |
| isBodyAndPreferenceSort | Boolean                   | 体型登録済みバナーのラベルを「体型と好みで並べ替え」（`true`）/「体型で並べ替え」（`false`）で出し分けるために使用します。<br>※ `isBodyRegistered` が `true` の場合のみ有効です。 |

###### Kotlin

```kotlin
coordinate.listener = object : jp.co.makip.unisizesdk.aunn.coordinate.AunnCoordinateListener {
    override fun onCoordinatesLoaded(
        coordinates: List<jp.co.makip.unisizesdk.aunn.coordinate.AunnCoordinateItem>,
        recType: jp.co.makip.unisizesdk.aunn.coordinate.AunnCoordinateRecommendAlgorithm,
        hasMore: Boolean,
        isBodyRegistered: Boolean,
        isBodyAndPreferenceSort: Boolean,
    ) {
        ・・・・ ここに記述
    }
    ・・・・ その他コード
}
```

#### onBeidChanged(beid: String)

beid が確定・変更されたときに実行したい処理を実装します。
unisize バナーを同居させない画面で、コンバージョン用に beid の変更を検知する用途を想定しています。

| パラメーター | 型     | 説明                       |
| :----------- | :----- | :------------------------- |
| beid         | String | 確定・変更された beid の値 |

- 実装は任意です。

#### onFail(error: UnisizeError)

エラーが発生した場合に実行したい処理を実装します。

| パラメーター | 型           | 説明                                                                                                                       |
| :----------- | :----------- | :-------------------------------------------------------------------------------------------------------------------------- |
| error        | UnisizeError | AunnCoordinate クラスから返された UnisizeError class を取得できます。<br>（※ UnisizeError class の変数、関数は下記 UnisizeError class 参照） |

###### Kotlin

```kotlin
coordinate.listener = object : jp.co.makip.unisizesdk.aunn.coordinate.AunnCoordinateListener {
    override fun onFail(error: jp.co.makip.unisizesdk.UnisizeError) {
        ・・・・ ここに記述
    }
    ・・・・ その他コード
}
```

## AunnCoordinateTracking Class

| Class        | 継承 | タイプ |
| :----------- | :--- | :----- |
| AunnCoordinateTracking | なし | class  |

コーディネート詳細画面 / スタッフ画面の表示計測を行う Class。
コーディネート一覧から遷移した先の画面の Activity（または、Fragment）に実装します。

- インスタンス生成時に `Context` を渡します。（例：`val tracking = AunnCoordinateTracking(context)`）

### メソッド、プロパティ

| メソッド、プロパティ                                    | 説明                                          | 種類     |
| :------------------------------------------------------ | :--------------------------------------------- | :------- |
| [setupParam()](#setupparam-メソッドaunncoordinatetrackingparam)   | AunnCoordinateTracking Class のパラメーターを設定します。 | メソッド |
| [send()](#send-メソッド)                                | 表示計測を送信します。                        | メソッド |

### setupParam() メソッド（AunnCoordinateTrackingParam）

AunnCoordinateTracking Class のパラメーターを `AunnCoordinateTrackingParam` で設定します。
`send()` を呼び出す前に必ず実行してください。

| パラメーター   | 型           | 初期値 | 説明                                                                                                                                 |
| :------------- | :----------- | :----- | :------------------------------------------------------------------------------------------------------------------------------------ |
| cid            | String       | -      | クライアント ID（必須）                                                                                                              |
| pageType       | AunnCoordinatePageType | -      | 画面の種別（必須）。`COORDINATE_DETAIL`（コーディネート詳細画面）または `STAFF`（スタッフ画面）を指定します。                              |
| cuid           | String?      | null   | クライアント会員 ID（EC アプリケーション（サイト）のユーザーログイン ID）                                                            |
| coordinateId   | Int?         | null   | コーディネート ID<br>※ pageType が `COORDINATE_DETAIL` の場合に指定します。                                                                |
| staffId        | Int?         | null   | スタッフ ID<br>※ pageType が `STAFF` の場合に指定します。                                                                            |
| enablePrintLog | Boolean      | false  | AunnCoordinateTracking Class のデバッグログを Android Studio の Logcat に出力します。<br>※ リリース時は `false` を指定してください。            |

### send() メソッド

表示計測を 1 回送信します。

- パラメーターなし

## AunnCoordinateMeasurement Class

| Class           | 継承    | タイプ |
| :-------------- | :------ | :----- |
| AunnCoordinateMeasurement | WebView | class  |

体型登録アプリを表示する WebView Class。
体型登録バナーがタップされたときに、全画面（Dialog / 専用 Activity など）で表示します。

- インスタンス生成時に `Context` を渡します。（例：`val measurement = AunnCoordinateMeasurement(context)`）

### メソッド、プロパティ

| メソッド、プロパティ                                        | 説明                                                                       | 種類       |
| :---------------------------------------------------------- | :--------------------------------------------------------------------------- | :--------- |
| [open()](#open-メソッド)                                    | 体型登録アプリを起動します。                                               | メソッド   |
| [onDestroy()](#ondestroy-メソッド)                          | WebView を破棄します。                                                     | メソッド   |
| [listener](#listener-プロパティaunncoordinatemeasurementlistener)     | 体型登録完了時、エラー発生時などのイベントをハンドリングするためのリスナーを設定します。 | プロパティ |

### open() メソッド

体型登録アプリを起動します。beid は SDK が自動で引き継ぐため、ホストアプリで受け渡す必要はありません。

| パラメーター   | 型            | 初期値 | 説明                                                                                                                      |
| :------------- | :------------ | :----- | :-------------------------------------------------------------------------------------------------------------------------- |
| cid            | String        | -      | クライアント ID（必須）                                                                                                   |
| itemId         | String?       | null   | アイテム識別 ID（商品 ID）                                                                                                |
| cuid           | String?       | null   | クライアント会員 ID（EC アプリケーション（サイト）のユーザーログイン ID）                                                 |
| pageType       | AunnCoordinatePageType? | null   | 体型登録アプリを開いた画面の種別。計測データの画面区分に使用します。                                                      |
| enablePrintLog | Boolean       | false  | AunnCoordinateMeasurement Class のデバッグログを Android Studio の Logcat に出力します。<br>※ リリース時は `false` を指定してください。 |
| sendErrorLog   | Boolean       | true   | エラー発生時にエラーログを unisize サーバーへ送信します。                                                                 |

- 体型登録アプリのオープン計測・完了計測は、この Class が自動で送信します。ホストアプリから明示的に呼び出す必要はありません。

### onDestroy() メソッド

WebView を完全に破棄します。モーダルを閉じるとき（Dialog の dismiss 時など）に**必ず**呼び出してください。

- パラメーターなし
- 呼び出さない場合、ネイティブ側の WebView が GC まで残り続けます。体型登録アプリを連続で開くとインスタンスが累積します。
- 呼び出したあとのインスタンスは**再利用できません**。再表示するときは新規に `AunnCoordinateMeasurement` を生成してください。

### listener プロパティ（AunnCoordinateMeasurementListener）

体型登録の完了時、閉じられたとき、エラー発生時に、EC アプリケーション側で処理を行うため、listener 内に実装を行います。

| メソッド                    | 説明                                                                       |
| :-------------------------- | :--------------------------------------------------------------------------- |
| onEnqueteCompleted()        | 体型登録が完了したときに実行したい処理を実装します。                       |
| onClosed()                  | 「購入を続ける」で閉じられたときに実行したい処理を実装します。         |
| onBeidChanged(beid: String) | beid が差し替えられたときに実行したい処理を実装します。                    |
| onFail(error: UnisizeError) | エラーが発生した場合に実行したい処理を実装します。                         |

- いずれのメソッドも実装は任意です。
- **バックキーや画面外のタップで閉じられた場合、通知されるコールバックはありません。** 体型登録アプリを閉じたあとの後片付け（`onDestroy()` の呼び出しなど）は、これらのコールバックではなく `Dialog` の `setOnDismissListener` など、閉じるすべての経路が通る場所へまとめて実装してください。

#### onEnqueteCompleted()

体型登録が完了したときに実行したい処理を実装します。
コーディネートの並び順が体型マッチ順に変わるため、`AunnCoordinate.reload()` を呼び出してコーディネートを再取得してください。

- パラメーターなし

###### Kotlin

```kotlin
measurement.listener = object : jp.co.makip.unisizesdk.aunn.coordinate.AunnCoordinateMeasurementListener {
    override fun onEnqueteCompleted() {
        coordinate.reload()
        ・・・・ ここに記述
    }
    ・・・・ その他コード
}
```

#### onClosed()

体型登録アプリの「購入を続ける」で閉じられたときに実行したい処理を実装します。

- パラメーターなし
- バックキーや画面外のタップで閉じられた場合は呼び出されません。

#### onBeidChanged(beid: String)

beid が差し替えられたときに実行したい処理を実装します。
unisize バナーを同居させない画面で、コンバージョン用に beid の変更を検知する用途を想定しています。

| パラメーター | 型     | 説明                     |
| :----------- | :----- | :----------------------- |
| beid         | String | 差し替えられた beid の値 |

#### onFail(error: UnisizeError)

エラーが発生した場合に実行したい処理を実装します。

| パラメーター | 型           | 説明                                                                                                                        |
| :----------- | :----------- | :---------------------------------------------------------------------------------------------------------------------------- |
| error        | UnisizeError | AunnCoordinateMeasurement クラスから返された UnisizeError class を取得できます。<br>（※ UnisizeError class の変数、関数は下記 UnisizeError class 参照） |

- エラーが発生しても SDK は体型登録アプリを閉じません。そのままでは操作できない画面が残るため、ホストアプリ側で閉じる処理を実装してください。

## aunn コーディネートのモデル / enum

`AunnCoordinateListener.onCoordinatesLoaded()` で受け取るコーディネート配列のモデルと、パラメーターに指定する enum です。

### AunnCoordinateItem

コーディネート 1 件を表す data class。

| プロパティ | 型        | 説明                                       |
| :--------- | :-------- | :----------------------------------------- |
| id         | Int       | コーディネート ID                                  |
| size       | String    | スタッフが着用しているサイズ               |
| imgUrl     | String    | コーディネート画像の URL                           |
| staff      | AunnCoordinateStaff | コーディネートを着用しているスタッフ               |
| brand      | AunnRef   | ブランド                                   |
| shop       | AunnRef   | 店舗                                       |
| hasVideo   | Boolean   | 動画が紐づいているか（初期値 `false`）     |

### AunnCoordinateStaff

スタッフ情報を表す data class。

| プロパティ    | 型      | 説明                     |
| :------------ | :------ | :----------------------- |
| id            | Int     | スタッフ ID              |
| code          | String  | スタッフコード           |
| name          | String  | スタッフ名               |
| height        | Int     | 身長（cm）               |
| imgUrl        | String  | スタッフ画像の URL       |
| bodyType      | String? | 体型タイプ               |
| personalColor | String? | パーソナルカラー         |

### AunnRef

ブランド / 店舗を表す data class。

| プロパティ | 型     | 説明     |
| :--------- | :----- | :------- |
| id         | Int    | ID       |
| code       | String | コード   |
| name       | String | 名称     |

### AunnCoordinatePageType

コーディネート一覧を表示する画面、または計測対象の画面の種別を表す enum。

| 値                    | 説明                                                       |
| :-------------------- | :--------------------------------------------------------- |
| `TOP`                 | トップ画面                                                 |
| `COORDINATE`        | コーディネート一覧画面                                             |
| `COORDINATE_ITEM`   | コーディネート一覧画面（商品詳細画面から遷移した場合）             |
| `ITEM_DETAIL`         | 商品詳細画面                                               |
| `COORDINATE_DETAIL` | コーディネート詳細画面（`AunnCoordinateTracking` で使用）                    |
| `STAFF`               | スタッフ画面（`AunnCoordinateTracking` で使用）                      |

- `ITEM_DETAIL` / `COORDINATE_ITEM` を指定する場合は、`itemId` の指定が必須です（`requiresItemId()` で判定できます）。
- 各値は文字列表現を `value` プロパティで取得でき、`AunnCoordinatePageType.fromValue(value)` で文字列から復元できます。画面遷移をまたいで値を保持する場合などにご利用ください。

### AunnCoordinateRecommendAlgorithm

コーディネート一覧に適用されたレコメンドアルゴリズムを表す enum。`onCoordinatesLoaded()` で受け取り、計測メソッドへそのまま渡します。

| 値                          | 説明                       |
| :-------------------------- | :------------------------- |
| `NONE`                      | 指定なし                   |
| `PUBLISHED_AT`              | 公開日順                   |
| `PV_RANKING`                | PV ランキング順            |
| `COLLABORATIVE_FILTERING`   | 協調フィルタリング         |
| `BODY_MATCHING`             | 体型マッチ                 |
| `BODY_MATCHING_AND_CF`      | 体型マッチ ＋ 協調フィルタリング |

- 各値は文字列表現を `value` プロパティで取得でき、`AunnCoordinateRecommendAlgorithm.fromValue(value)` で文字列から復元できます（該当しない文字列の場合は `NONE` を返します）。画面遷移をまたいで値を保持する場合などにご利用ください。

## UnisizeError Class

| Class        | 継承 | タイプ     |
| :----------- | :--- | :--------- |
| UnisizeError | なし | data class |

UnisizeSDK でエラーが発生した際に、リスナーで受け取ることができるエラー情報。
エラー発生時にこの class からエラー関連の情報などを取得することができます。
各コンポーネントのリスナーの didFail で使用できます。

| メソッド、プロパティ                       | 説明                                     | 種類     |
| :----------------------------------------- | :--------------------------------------- | :------- |
| [getJson()](#getjson-メソッド)             | エラー情報を JSONObject 形式で返します。 | メソッド |
| [getJsonString()](#getjsonstring-メソッド) | エラー情報を Json String で返します。    | メソッド |

### getJson() メソッド

UnisizeSDK のエラー発生時に、エラーに関する全ての情報を JSONObject 形式で取得できます。

###### Kotlin

```kotlin
// エラーが発生した場合に実行したい処理を記述
override fun didFail(unisizeError: jp.co.makip.unisizesdk.UnisizeError) {
    val errObj = unisizeError.getJson()
}
```

###### Java

```Java
// エラーが発生した場合に実行したい処理を記述
@Override
public void didFail(jp.co.makip.unisizesdk.UnisizeError unisizeError) {
    JSONObject errObj = unisizeError.getJson();
}
```

### getJsonString() メソッド

UnisizeSDK のエラー発生時に、エラーに関する全ての情報を JSON テキスト形式で取得できます。

###### Kotlin

```kotlin
// エラーが発生した場合に実行したい処理を記述
override fun didFail(unisizeError: jp.co.makip.unisizesdk.UnisizeError) {
    val jsonString = unisizeError.getJsonString()

}
```

###### Java

```Java
// エラーが発生した場合に実行したい処理を記述
@Override
public void didFail(jp.co.makip.unisizesdk.UnisizeError unisizeError) {
    String jsonString = unisizeError.getJsonString()

}
```
